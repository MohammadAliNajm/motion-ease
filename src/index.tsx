import React from 'react'
import Button from './components/Button/Button'

type Props = {}

const index = (props: Props) => {
  return (
    <div>
        <Button successColor={''} success={false}   />
    </div>
  )
}

export default index